# VoCoType Linux 输入法依赖（IBus / Fcitx 5 backend）
# 注：pyproject.toml 为标准安装入口；本文件保留用于 pinned 安装

# 音频采集和处理
sounddevice==0.5.2
librosa==0.11.0
soundfile==0.13.1

# 语音识别核心
funasr_onnx==0.4.2
jieba==0.42.1

# IBus Python 绑定 (Linux 必需)
# 3.52+ 改用 girepository-2.0，并要求 GLib >= 2.80；Ubuntu 22.04 不满足。
# 3.46-3.50 同时支持 Python 3.12 与传统 gobject-introspection-1.0。
PyGObject>=3.46,<3.52

# 模型下载 (推荐)
modelscope==1.30.0

# 说明：
# - 默认使用 ONNX 路线；启用本地 SLM 需要 torch（CPU/GPU 版本按需选择）
# - PyGObject 需要系统包：python3-gi, gir1.2-ibus-1.0, gobject-introspection
