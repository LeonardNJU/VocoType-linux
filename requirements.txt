# VoCoType Linux 输入法依赖（IBus / Fcitx 5 backend）
# 注：pyproject.toml 为标准安装入口；本文件保留用于 pinned 安装

# 音频采集和处理
sounddevice==0.5.2
numpy==1.26.4
scipy==1.16.3
soundfile==0.13.1

# 语音识别核心
funasr-onnx==0.4.2
jieba==0.42.1
PyYAML>=6.0,<7

# IBus Python 绑定 (Linux 必需)
# Release CI 会针对 DEB/RPM/Arch 的系统 GI 库分别构建兼容 wheel。
# 用户安装只消费包内 wheelhouse，不在本机编译 PyGObject。
PyGObject>=3.46

# 模型下载 (推荐)
modelscope==1.30.0

# 说明：
# - 默认使用 ONNX 路线；AI 润色只调用用户配置的 OpenAI-compatible API
# - PyGObject 需要系统包：python3-gi, gir1.2-ibus-1.0, gobject-introspection
